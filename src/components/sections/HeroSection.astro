---
<<<<<<< HEAD
import heroData from '../../data/content.hero.json';
import SmoothScroll from '../ui/atoms/SmoothScroll.astro';
import HeroImage from '../ui/atoms/HeroImage.astro';
import HeroBadge from '../ui/atoms/HeroBadge.astro';
import HeroContent from '../ui/molecules/HeroContent.astro';
=======
import heroMaskDesktop from '../../assest/hero/hero-mask-desktop.svg?url';
import heroMaskTablet from '../../assest/hero/hero-mask-tablet.svg?url';
import heroMaskMobile from '../../assest/hero/hero-mask-mobile.svg?url';
import heroImage from '../../assest/hero/hero-bg.png?url';
import logoIcon from '../../assest/common/logo-icon.svg?url';
import menuIcon from '../../assest/common/menu-icon.svg?url';
import heroData from '../../data/content.hero.json';
import SmoothScroll from '../ui/atoms/SmoothScroll.astro';
import Claim from '../ui/atoms/Claim.astro';
import CTAButton from '../ui/atoms/CTAButton.astro';
>>>>>>> 61b8518 (update 080426)
import Header from '../ui/organisms/Header.astro';
const { content } = heroData;
---

<<<<<<< HEAD
<section class="hero-section mx-auto flex w-full max-w-[1920px] flex-col items-center gap-10 px-5 pb-[11px] pt-[20px] md:pt-[20px] lg:pt-[60px]">
  <!-- ========== HEADER ========== -->
  <Header /> 

=======
<section class="hero-section mx-auto flex w-full max-w-[1920px] flex-col items-center gap-16 px-5 pb-[11px] pt-[60px]">
  <!-- ========== HEADER ========== -->
  <Header />

  <!-- ========== HERO CONTENT ========== -->
>>>>>>> 61b8518 (update 080426)
  <!-- Desktop Hero -->
  <div class="relative hidden w-full max-w-[1700px] overflow-hidden rounded-[60px] bg-white lg:block">
    <div class="relative flex h-[775px] w-full items-center justify-end">
      <!-- Hero Image Container -->
      <HeroImage 
        containerClass="relative overflow-hidden h-[775px] w-[907px]"
        screen="desktop"
      />

      <!-- Content Container -->
      <div class="absolute left-10 top-0 flex items-end gap-10 pb-10">
        <!-- Badge rotado "1000+ Clientes" -->
<<<<<<< HEAD
        <HeroBadge text={content.badge.text} />

        <!-- Hero Content -->
        <HeroContent 
          bannerText={content.banner.text}
          taglineLines={content.tagline.lines}
          claimText={content.claim}
          ctaHref={content.cta.href}
          ctaText={content.cta.text}
          screen="desktop"
        />
=======
        <div class="flex h-[434px] w-[92px] items-center justify-center">
          <div class="-rotate-90">
            <div class="flex w-[434px] items-center justify-center gap-[10px] rounded-[60px] p-[10px]" style="background: radial-gradient(ellipse at center, rgba(255,255,255,1) 55.77%, rgba(239,239,239,1) 90.38%, rgba(235,235,235,1) 97.6%);">
              <div class="relative h-[72px] w-[152px]">
                <span class="absolute left-0 top-0 h-[72px] w-[72px] rounded-full bg-gradient-to-r from-[#e87272] to-[#e87272]"></span>
                <span class="absolute left-10 top-0 h-[72px] w-[72px] rounded-full bg-[var(--color-azulhorizonte60)]"></span>
                <span class="absolute left-20 top-0 h-[72px] w-[72px] rounded-full bg-[var(--color-verdevitalidad-light)]"></span>
              </div>
              <div class="flex w-[252px] flex-col items-center justify-center py-[14px]">
                <p class="whitespace-nowrap text-[20px] font-semibold text-[var(--color-black)]">{content.badge.text}</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Hero Content -->
        <div class="flex w-[570px] flex-col items-center gap-[72px]">
          <!-- Banner -->
          <div class="flex h-[51px] w-full items-center justify-center gap-[10px] rounded-[12px] bg-white p-[10px]">
            <span class="h-10 w-10 rounded bg-[var(--color-verdevitalidad)]"></span>
            <p class="whitespace-nowrap text-[24px] font-extrabold text-[var(--color-verdevitalidad)]">{content.banner.text}</p>
          </div>

          <!-- Tagline -->
          <div class="flex h-[300px] items-start">
            <div class="flex w-[570px] items-center justify-center self-stretch">
              <h1 class="whitespace-nowrap text-[96px] leading-[100px]">
                {content.tagline.lines.map((line, index) => (
                  <span class={`block ${index === 0 ? 'font-bold text-[var(--color-azulhorizonte-alt)]' : 'font-semibold text-[var(--color-black)]'}`}>{line}</span>
                ))}
              </h1>
            </div>
          </div>

          <!-- Claim -->
          <div class="h-[113px] w-full">
            <Claim text={content.claim} className="h-6 w-[570px]" />
          </div>

          <!-- CTA Button -->
          <CTAButton href={content.cta.href} text={content.cta.text} />
        </div>
>>>>>>> 61b8518 (update 080426)
      </div>
    </div>
  </div>

  <!-- Tablet Hero -->
  <div class="relative hidden  w-full max-w-[831px] overflow-hidden rounded-[60px] mb-10 bg-white md:block lg:hidden">
    <div class="relative flex h-auto min-h-[600px] w-full items-center justify-end">
      <!-- Hero Image tablet-->
      <HeroImage 
        containerClass="relative overflow-hidden h-[775px] w-[907px]"
        screen="tablet"
      />

      <!-- Content centered -->
<<<<<<< HEAD
      <HeroContent 
        bannerText={content.banner.text}
        taglineLines={content.tagline.lines}
        claimText={content.claim}
        ctaHref={content.cta.href}
        ctaText={content.cta.text}
        screen="tablet"
      />

=======
      <div class="absolute left-1/2 top-1/2 z-10 flex -translate-x-1/2 -translate-y-1/2 flex-col items-center gap-16">
        <!-- Banner -->
        <div class="flex h-[51px] items-center justify-center gap-[10px] rounded-[12px] bg-[var(--color-verdevitalidad)] p-[10px]">
          <span class="h-10 w-10 rounded bg-white"></span>
          <p class="whitespace-nowrap text-[24px] font-extrabold text-white">{content.banner.text}</p>
        </div>

        <!-- Tagline -->
        <div class="flex h-[249px] items-center justify-center">
          <h1 class="whitespace-nowrap text-center text-[80px] leading-[83px]">
            {content.tagline.lines.map((line, index) => (
              <span class={`block ${index === 0 ? 'font-bold text-[var(--color-azulhorizonte-alt)]' : 'font-semibold text-[var(--color-black)]'}`}>{line}</span>
            ))}
          </h1>
        </div>

        <!-- Claim -->
        <div class="h-[113px] w-[570px]">
          <Claim text={content.claim} className="text-center" />
        </div>

        <!-- CTA Button -->
        <CTAButton href={content.cta.href} text={content.cta.text} />
      </div>
>>>>>>> 61b8518 (update 080426)
    </div>
  </div>

  <!-- Mobile Hero -->
  <div class="relative block w-full max-w-[500px] overflow-hidden rounded-[28px] mb-5 bg-white md:hidden">
    <div class="relative flex h-fit w-full items-center justify-top">
      <!-- Hero Image Container -->
      <HeroImage 
        containerClass="relative overflow-hidden h-[625px] w-[907px]"
        screen="mobile"
      />
      <!-- Content centered -->
<<<<<<< HEAD
      <HeroContent 
        bannerText={content.banner.text}
        taglineLines={content.tagline.lines}
        claimText={content.claim}
        ctaHref={content.cta.href}
        ctaText={content.cta.text}
        screen="mobile"
      />
=======
      <div class="absolute left-1/2 top-1/2 z-10 flex -translate-x-1/2 -translate-y-1/2 flex-col items-center gap-10 px-5">
        <!-- Banner -->
        <div class="flex h-[51px] items-center justify-center gap-[10px] rounded-[12px] bg-white p-[10px]">
          <span class="h-10 w-10 rounded bg-[var(--color-verdevitalidad)]"></span>
          <div class="text-center text-[16px] font-extrabold leading-normal text-[var(--color-verdevitalidad)]">
            {content.mobile.banner.lines.map((line) => (
              <p>{line}</p>
            ))}
          </div>
        </div>

        <!-- Tagline -->
        <div class="flex h-[180px] items-center justify-center">
          <h1 class="whitespace-nowrap text-center text-[52px] leading-[60px]">
            {content.tagline.lines.map((line, index) => (
              <span class={`block ${index === 0 ? 'font-bold text-[var(--color-azulhorizonte-alt)]' : 'font-semibold text-[var(--color-black)]'}`}>{line}</span>
            ))}
          </h1>
        </div>

        <!-- Claim -->
        <div class="w-full">
          <Claim text={content.claim} className="text-center" />
        </div>

        <!-- CTA Button -->
        <a href={content.cta.href} class="flex h-[55px] w-[217px] items-center justify-center rounded-lg bg-[var(--color-azulhorizonte-alt)] p-3 text-[20px] font-semibold text-white transition-colors hover:bg-[var(--color-azulhorizonte)]">
          {content.cta.text}
        </a>
      </div>
>>>>>>> 61b8518 (update 080426)
    </div>
  </div>

</section>

<<<<<<< HEAD
=======
<style>
  .hero-image-container {
    position: relative;
    overflow: hidden;
  }

  .hero-mask-desktop {
    position: absolute;
    left: 0;
    top: 0;
  }
</style>

>>>>>>> 61b8518 (update 080426)
<SmoothScroll />
