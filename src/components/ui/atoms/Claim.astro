---
export interface Props {
  text: string;
  className?: string;
}

const { text, className = '' } = Astro.props;
---

<p class={`text-[20px] font-medium leading-7 text-[var(--color-gray600)] ${className}`}>
  {text}
</p>
